# Tasks

- [x] Create Bead `llm-council-s0h` and approved OpenSpec proposal, design, tasks, and delta specs.
- [x] Delete obsolete root `TASK.md`; this change and Bead `llm-council-s0h` supersede it.
- [ ] Add registry schema, frozen registry fixture, validation, import-safety tests, and stable logical/route ID tests.
- [ ] Add append-only lifecycle/evidence schemas, persistence, transition validation, and transition tests.
- [ ] Implement immutable execution planning, deterministic diversity curation, and structured unsatisfied-constraint errors.
- [ ] Consolidate provider adapters behind one dispatcher used by sync, stream, and benchmarks.
- [ ] Add sync/stream semantic parity, immutable-plan, route-equivalence, fallback, and failure tests.
- [ ] Generate backend, API, frontend, MCP, and benchmark projections; add CI drift and hard-coded-roster checks.
- [ ] Create and review a versioned 60–100-prompt public/non-PHI benchmark suite with objective validators and two-family subjective judging.
- [ ] Implement balanced/resumable benchmark artifacts, budget limits, reporting, and seat-ablation analysis.
- [ ] Implement conditional Parallel Search Stage 0 with explicit URL fetch preservation, gating, provenance, caps, failure-open behavior, and injection-safety tests.
- [ ] Implement Parallel Monitor event/schema validation, deduplication, and candidate-proposal-only output with non-mutation tests.
- [ ] Run all local backend, frontend, MCP, benchmark, security, and OpenSpec validation gates.
- [ ] Deploy shadow and canary, evaluate acceptance gates, rehearse rollback, then deploy production with pinned registry/artifact digests.
- [ ] Verify the frozen roster and public/non-PHI safety boundary in production smoke checks.
- [ ] Open and merge the reviewed Forgejo PR after CI passes; archive this change and close `llm-council-s0h` only after deployment acceptance.
